mutation MoveTask($input: UpdateTaskInput!) {
   updateTask(input: $input) {
      id
      name
      tags
      status
      dueDate
      position
      pointEstimate
      assignee {
         id
         avatar
         fullName
      }
   }
}
